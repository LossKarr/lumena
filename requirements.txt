# LUMENA - Dépendances Python
# ============================

# Core
httpx>=0.28.0
aiohttp>=3.9.0
pydantic>=2.0.0
python-dotenv>=1.0.0

# LLM Local
ollama>=0.1.0

# Mémoire avancée
chromadb>=0.4.0
mem0ai>=0.1.0
watchdog>=6.0.0
pyright>=1.1.0

# Voice
edge-tts>=6.1.0
faster-whisper>=0.10.0
nvidia-cublas-cu12         # DLL cublas64_12 requise par ctranslate2/faster-whisper GPU
nvidia-cudnn-cu12          # DLL cudnn requise par ctranslate2/faster-whisper GPU
SpeechRecognition>=3.10.0 # Calibration & Google Fallback
PyAudio>=0.2.14            # Microphone handling
pyttsx3>=2.90              # TTS fallback offline
piper-tts>=1.4.1           # Local TTS ONNX
pathvalidate>=3.2.0        # Dépendance Piper
pygame>=2.5.2              # Playback audio local

# Scheduler (CRON)
croniter>=6.0.0            # Support expressions CRON

# Monitoring système
psutil>=5.9.0              # RAM, CPU, disque (ops handlers)

# Computer Use
pyautogui>=0.9.54
keyboard>=0.13.5
mouse>=0.7.1
pywinauto>=0.6.8           # Windows UI Automation (contrôle par accessibilité, sans coords)

# Browser Automation
playwright>=1.40.0
selenium>=4.20.0

# Telegram
python-telegram-bot>=20.0

# Discord
discord.py>=2.7.1

# Peer-to-Peer (découverte mDNS/Zeroconf des autres Lumena — auto-jumelage zéro-config).
# Optionnel à l'exécution : le code dégrade proprement si absent (fallback scan LAN),
# mais présent par défaut pour l'expérience "tout seul" out-of-the-box.
zeroconf>=0.131.0

# Perception
Pillow>=10.0.0
mss>=9.0.0
pytesseract>=0.3.10         # OCR fallback (requiert Tesseract-OCR installé sur le système)

# Utils
rich>=13.0.0
typer>=0.9.0
loguru>=0.7.0

# Unity Bridge
websockets>=12.0

# Documents (PDF / DOCX / XLSX / PPTX / Conversion)
reportlab>=4.0.0           # PDF (déjà installé — génération de PDFs)
pypdf>=3.0.0               # PDF (déjà installé — lecture/fusion de PDFs)
pypdfium2>=5.11.0          # Rasterisation locale des apercus PDF Document Studio
python-docx>=1.1.0         # Fichiers Word .docx
openpyxl>=3.1.0            # Fichiers Excel .xlsx
python-pptx>=0.6.23        # Présentations PowerPoint .pptx
Jinja2>=3.1.3              # Templates documents (Jinja2)
weasyprint>=61.0           # HTML→PDF professionnel
mammoth>=1.8.0             # DOCX→HTML conversion sémantique

# Web Search
ddgs>=0.1.0                 # Recherche web gratuite via DuckDuckGo (nouveau package)
beautifulsoup4>=4.12.0      # HTML parsing (web_crawler, perception)
rank-bm25>=0.2.2            # BM25 search index (memory)
# duckduckgo-search>=6.0.0   # Ancien package (fallback si ddgs absent)

# Spotify
spotipy>=2.23.0             # Contrôle Spotify via API officielle

# Payments
stripe>=8.0.0               # Stripe API (produits, prix, paiements, abonnements)

# Web Server
fastapi>=0.100.0
uvicorn>=0.24.0
Flask>=3.1,<4.0             # Preview/runtime des applications Flask generees
python-multipart>=0.0.6    # FastAPI file upload (UploadFile)
filelock>=3.12.0

# Optionnel - Agent framework
# langchain>=0.1.0

# Linter (CodeAgent auto-lint)
ruff>=0.5.0

# Type checking (CodeAgent auto-typecheck)
mypy>=1.10.0

# Token counting précis
tiktoken>=0.7.0

# Desktop (fenêtre native, optionnel)
pywebview>=4.0.0

# Hosting / Deploy
paramiko>=3.0.0
# BDD associée aux sites IONOS (MariaDB/MySQL) — pur Python
pymysql>=1.1.0

# Twitter/X integration
tweepy>=4.14.0

# Pour fine-tuning local voir requirements-finetuning.txt
